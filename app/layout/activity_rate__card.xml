<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Customer.Rate_Card">

    <RelativeLayout
        android:id="@+id/ToolbarLayoutRC"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <android.support.v7.widget.Toolbar
            android:id="@+id/RateCardToolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/MediumBlue"
            android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
            app:title="@string/rate_card"
            app:navigationIcon="@drawable/ic_left_arrow_white"
            app:titleTextColor="@color/White">
        </android.support.v7.widget.Toolbar>
    </RelativeLayout>

    <RelativeLayout
        android:layout_below="@+id/ToolbarLayoutRC"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/White">

        <android.support.design.widget.TabLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/TabLayoutRC"
            android:background="@color/MediumBlue"
            app:tabSelectedTextColor="@color/White"
            app:tabIndicatorColor="@color/White"
            app:tabTextColor="@color/LightMediumBlue">
        </android.support.design.widget.TabLayout>

        <android.support.v4.view.ViewPager
            android:layout_width="match_parent"
            android:layout_height="fill_parent"
            android:layout_below="@+id/TabLayoutRC"
            android:id="@+id/pagerRC">
        </android.support.v4.view.ViewPager>
    </RelativeLayout>

</RelativeLayout>